export default interface Idog {
    // default - you dont need than to {} on the import statement
    id: string;
    race: string;
    gender: string;
    age: number;
    vaccines: number;
    behave: string[];
    image: string;
    name: string;
    status: string;
}
